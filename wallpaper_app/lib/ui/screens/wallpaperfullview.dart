import 'package:flutter/material.dart';
class WallpaperFullView extends StatelessWidget {
  const WallpaperFullView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: CircularProgressIndicator(),
    );
  }
}
